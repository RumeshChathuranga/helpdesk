# k3d overrides for the database release. 1 instance is a laptop concession —
# bumping to 3 is the only change needed for real HA.
instances: 1

storage:
  size: 2Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
