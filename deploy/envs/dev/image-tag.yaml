# Machine-owned — the bump-dev-tag job in ci.yml rewrites this after every
# push to master. Kept apart from values.yaml so the bump stays a one-line diff.
image:
  tag: sha-384f64941ee02dc7de214062090deeef283b8626
