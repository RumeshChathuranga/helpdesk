# Machine-owned. The bump-dev-tag job in .github/workflows/ci.yml rewrites this
# after every push to master. Kept apart from values.yaml so an automated
# rewrite can never reformat a file a human maintains — and so the bump commit
# is a one-line diff you can read at a glance.
image:
  tag: sha-74c63d30fba8ec8c6c06639590973022a530ce34
